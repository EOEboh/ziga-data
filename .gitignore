# Secrets — never commit
# *.env covers .env and the filled-in server template deploy/ziga.env.
# .env.example does not match (it does not end in .env) and stays tracked.
*.env
service-account*.json

# Local state
*.db

# Build output
/ziga
/dist/
node_modules/
